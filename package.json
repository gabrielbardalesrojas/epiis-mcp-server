{
  "name": "epiis-mcp-fullstack",
  "version": "1.0.0",
  "description": "Sistema de gestión académica EPIIS - Control centralizado",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run api:dev --prefix backend\" \"npm run dev --prefix frontend\" \"ngrok http --domain=krystina-weedier-howard.ngrok-free.dev https://localhost:3000\"",
    "start": "npm run dev",
    "setup": "npm run setup --prefix backend",
    "cleanup": "bash ./cleanup.sh",
    "reset-db": "npm run reset-db --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
